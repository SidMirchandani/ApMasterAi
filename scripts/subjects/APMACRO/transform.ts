
export function transformCrackAP(html: string) {
  // APMACRO-specific transform logic for CrackAP → Block[]
  // This will parse the HTML and convert it to question blocks
  const blocks: any[] = [];
  
  // Add APMACRO-specific parsing logic here
  // For now, return empty array as placeholder
  
  return blocks;
}
